import ConfirmationForm from "../lib/components/confirmation"

export default function Confirmation() {
    return (
        <main className="flex flex-col bg-white min-h-screen">
            <ConfirmationForm />
        </main>
    )
}